//get - used to retrieve info
//data is sent via query string
//post - used to post data to the server
//used to write/create/update
//data is sent via request body
//<form method="get" or method="post"/>
//with express we use app.post(req.body...res.send(..))
//app.use(express.urlencoded({extended:true})) it is for req.body to work 
//app.use(express.json()) to handle json

//REST - set of rules for client server communication 
//CRUD - CREATE READ UPDATE DELETE OR DESTROY
GET /comments
POST /comments
GET /comments/:id
PATCH /comments/:id - update a comment
DELETE /comments/:id